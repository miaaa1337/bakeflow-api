version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: bakeflow_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bakeflow_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d bakeflow_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    container_name: bakeflow_api
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/bakeflow_db
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data: